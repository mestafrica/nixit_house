<!--a view partial-->

<%= form_for @faq do |f| %>
 
  <% if @faq.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@faq.errors.count, "error") %> prohibited
        this faq from being saved:
      </h2>
      <ul>
        <% @faq.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </p>
 
  <p>
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %> 
